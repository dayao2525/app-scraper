export class AnchorModel {
    constructor(
        id, 
        name,
        url,
        type,
    ) {
        this.id = id
        this.name = name
        this.url = url
        this.type = type
    }
}